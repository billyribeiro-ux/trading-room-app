{
  "name": "trading-room-app",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.18.0",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev:room": "pnpm --filter ./apps/room dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "check": "pnpm -r check"
  }
}
