.svelte-kit/
build/
coverage/
evidence-dumps/
node_modules/
playwright-report/
services/
static/
test-results/

# Evidence-derived Svelte whitespace participates in measured inline geometry.
# These files remain guarded by ESLint, svelte-check, the official autofixer,
# breakpoint contracts, and pixel-forensic browser gates instead of a rewriter.
src/**/*.svelte

# Generated by scripts/extract-manage-schema.mjs; exact bytes are a tested seal.
src/lib/room-settings-schema.ts

# Generated dependency graph; pnpm is its only writer.
pnpm-lock.yaml

# Captured from the LIVE original and asserted against by contract tests. These are evidence:
# reformatting changes the bytes the implementation is checked against, and one of them is
# SHA-256 pinned by manage-panel-bootstrap3-contract.test.ts.
evidence-manage-gaps-2026-08-11.json
evidence-bootstrap-3.3.7.css
evidence-page.manageSession.html

# The manage page's own tooltip system, captured 2026-08-12. Evidence: reformatting changes the
# bytes any contract test reads it for.
evidence-tooltips-manage-2026-08-12.json

# The vitest JSON report, written by `test:unit` and read back by
# `scripts/verify-documented-test-counts.mjs`. A GENERATED artifact: prettier should not be checking
# it, for the reason `AGENTS.md` gives about `captured-runtime-components.css` — "do not hand-edit
# generated artifacts", so do not gate their formatting either.
.vitest-report.json

# Two GENERATED sources, each carrying its own `GENERATED by scripts/…` header.
#
# Added 2026-08-26 after formatting them broke `api-post-routes-docs.test.ts` and
# `emoji-picker-contract.test.ts`, both of which re-run the generator and assert the output is
# byte-identical. Prettier's opinion and the generator's output are not the same bytes, so gating
# these on formatting guarantees one of the two gates is red. Same rule as `.vitest-report.json`
# above and as `AGENTS.md` records for `captured-runtime-components.css`: do not hand-edit generated
# artifacts, and do not check their formatting either.
src/lib/content/api-post-routes.ts
src/lib/content/emoji-picker.ts

# THE DECODED CAPTURES, 73 files. Evidence, and the largest single reason `format:check` had drifted
# to 88 failing files while nothing in CI ran it.
#
# `docs/reference/pieces/INDEX.md` states what these are in its own first paragraph: two reference
# captures of the live original "decoded into one file per component", each carrying "every
# attribute verbatim" and "resolved absolute computed styles". Reformatting them changes the bytes
# the implementation is checked against, which is the same rule `CLAUDE.md` states for the capture
# directories and that the evidence entries above already apply file by file.
#
# Added 2026-08-29 alongside the first repository-wide `format:check` measurement. The alternative -
# reformatting them - was refused rather than deferred: prettier rewrites tables and wraps prose,
# and a verbatim attribute table is not prose.
docs/reference/

# Written by `scripts/extract-*`; each carries its own generated header. Same rule as
# `.vitest-report.json` and the two generated sources above.
docs/generated/
