function X0e(t, n) {
  if (1 & t) {
    const e = Y();
    (d(0, 'button', 4),
      x('click', function () {
        return (D(e), E(g().stopYTForAll()));
      }),
      v(1, ' Stop For All'),
      u());
  }
}
