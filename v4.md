# v4 — what it is, and what it brings

Audited 2026-08-12, same pass as `v5.md`. Every claim carries a file:line citation.

---

## ⛔ OWNER DIRECTIVE — 2026-08-12, and it overrides the reproduction default

> **"WE SHOULD HAVE ALL THE VERSIONS BUT USE BY DEFAULT V4"**
> **"and have v5 working end to end in case we choose it"**

This is a **product decision, not a finding.** It is recorded here separately from the evidence
above so the two are never confused. Nothing in the dumps says this; the owner does.

What it means concretely:

1. **All three versions exist as settings** — `useV3`, `useV4`, `useV5`. `useV4` is therefore NOT
   dead markup for us, even though the reference comments it out. This repository diverges here
   **deliberately and on instruction.**
2. **v4 is the DEFAULT.** A room with nothing set runs v4. That is a real default to encode, not a
   fallback that happens to be reached.
3. **v5 must work end to end** — selectable, and fully functional when selected. Not stored-and-inert
   like the other 18 `dont-touch` settings currently are
   (`apps/controller/docs/OUTSTANDING.md:541-542`).

### What that makes outstanding

None of this exists yet. Honestly stated:

| # | work | status |
| --- | --- | --- |
| 1 | `useV4` row in `room-settings-schema.ts` + on the type + rendered in the settings page | **not started** — v3 and v5 are at `:291-292`, `:794-796`; v4 is absent |
| 2 | A default-version rule making v4 the default when unset | **not started** — no default logic exists for any of the three |
| 3 | Mutual exclusivity, if these are exclusive | **UNKNOWN** — the reference renders three independent checkboxes with no visible guard. Whether the server treats them as exclusive is not established. **Must be read before implementing**, because "three booleans" and "one enum" are different products. |
| 4 | v5 wired end to end | **not started, and not yet specifiable** — see the gap below |
| 5 | `dont-touch-block.test.ts` updated for a v4 row, and its ordering assertions checked | **not started** |

### The blocking unknown

**We still do not know what any of these versions DO.** `useV3`/`useV4`/`useV5` appear **zero** times
in the room bundle (control passed), so the switch is server-side and its meaning lives in code this
audit did not read. "Have v5 working end to end" cannot be built until it is known what v5 selects —
a media stack, a bundle, a cluster, or a protocol.

**The next read is `services/**`**, for `useV5` / `use_v5` / a session-version column or routing
branch. That is one read and it unblocks items 3 and 4.

If it is not there either, this becomes a collector job: capture the session record the API returns
for a room and read what the server actually persists. It must not click anything in the DON'T TOUCH
block — the warning is explicit that turning these on breaks the room.

---

## The short answer

**v4 is dead markup.** It exists in exactly one place in this entire repository: a commented-out
`<p>` in the reference's Manage Room template. It never renders, so it is not in any DOM capture, not
in the decoded field table, not in our schema, and not on our settings page.

**That is not a gap. It is a match.** The reference does not render it either, and our absence of it
is the correct reproduction.

---

## The only occurrence, verbatim

`apps/controller/evidence-page.manageSession.html:2304-2310` — note the enclosing `<!--` and `-->`:

```html
<!-- <p class="form-control-static">
    <label class="col-sm-2 control-label">Use v4? (DON'T!)</label>
    <a href="" onaftersave="saveSessField('useV4')" editable-checkbox="sess.useV4" e-title="Use v4?">
                                {{ sess.useV4 && "Yes!" || "No" }}
    </a>
    (DON'T TURN THIS ON, If PTR did not clear you for v4!! it will not work....)
</p> -->
```

It sits **between** the live `useV5` row (`:2297-2303`) and the live `clusterID` row (`:2313`), inside
the collapsed "DON'T TOUCH" block described in `v5.md`.

Structurally it is identical to its v3 and v5 siblings — same `p.form-control-static`, same
`label.col-sm-2.control-label`, same `editable-checkbox`, same `saveSessField(...)`, same
parenthetical warning with the version number swapped. Someone wrote it as a peer and then commented
it out.

---

## Where it is NOT — each one checked

| looked in | result |
| --- | --- |
| `apps/room/docs/source/main.d6d3c112b59b7d0d.js` (the room bundle, 2,887,876 bytes) | **0 occurrences.** Control: `sessData` matches in the same file by the same method, so the file was read. |
| `apps/controller/docs/reference/pieces/ptr1-P22-settings-advanced-cluster.md` | **Absent from the 49-field table** (`:139-187`). Expected — that decode is of a *rendered* DOM capture, and commented-out markup does not render. |
| `apps/controller/src/lib/room-settings-schema.ts` | **Absent.** `useV3` is at `:291`, `useV5` at `:292`; there is no `useV4` row and no `useV4` on the type at `:794-796`. |
| `apps/controller/src/lib/dont-touch-block.test.ts` | **Absent.** The block's contract test names `Note:useV3` (`:69`) and `Note:useV5` (`:70`) and nothing for v4. |
| `apps/controller/src/routes/(app)/account/rooms/[id]/[[tab]]/+page.svelte` | **Absent.** `useV3` at `:2605`, `useV5` at `:2609`, no v4. |
| `apps/controller/docs/OUTSTANDING.md` | **Absent.** `:541` names "`useV3`/`useV5`" only. |

Method: `grep -n` over each named file individually, plus direct reads of the regions. Not the
`grep -rl … --include=` form, which is broken in this environment (see the correction in `v5.md`).

---

## What v4 brings to the table

**Nothing, functionally.** There is no v4 code path, no v4 setting in any live surface, and no
captured value for it anywhere.

What it brings is **information**:

1. **The version line is v3 → v4 → v5, and v4 was retired.** The template kept the row rather than
   deleting it, which tells you v4 was real at some point and was withdrawn — not that it never
   existed. The wording of the warning ("If PTR did not clear you for v4!!") is identical in form to
   v3's and v5's, so it was operated the same way.
2. **It confirms how these switches are meant to be read.** v3, v4 and v5 are peers: per-room,
   server-persisted, gated on the account being "cleared", and warned about in identical language.
   None of them is a client feature flag.
3. **It is a deliberate divergence trap.** A future reader diffing the reference template against our
   schema will find `useV4` present there and absent here and may "fix" it by adding the field. That
   would ship a control the reference does not render. Hence this file.

---

## Should we reproduce it? — SUPERSEDED

**The evidence-only answer was no.** The commented-out row produces no DOM node and no behaviour, so
reproducing it would ship a control the reference does not render.

**The owner has overruled that, and is right to** — see the directive at the top. We are not only
reproducing the reference here; we are choosing a version to run. v4 becomes a real setting and the
default.

So the divergence is now intentional and documented, which is the difference between a divergence and
a defect. `dont-touch-block.test.ts` pins the block's contents, so **its ordering assertions must be
updated deliberately** when the v4 row lands — and checked that they assert the full ordered list
rather than mere presence, since only the former would catch an accidental insertion.

## Honest gaps

1. **What v4 was.** Nothing in this repository says. Same gap as v5's, and for the same reason: these
   are server-side switches and the server code was not read in this pass.
2. **When and why it was retired.** Not recoverable from a static template — it would need the
   reference's own version control, which we do not have.
3. **Whether the server still accepts `useV4`.** `services/**` was not read. If a room somewhere
   still has the flag set, only the backend knows.

---

## Relationship to `v5.md`

Read that one first. It carries the live values from the captured room — **`useV3 = Yes!`,
`useV5 = No`** (`ptr1-P22-settings-advanced-cluster.md:139-140,310`) — which is the most useful fact
in either document: the room this repository reproduces was running **v3**.
